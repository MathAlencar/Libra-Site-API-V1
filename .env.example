# Copie para .env e preencha. Os nomes precisam ser exatamente estes.

# Banco (src/config/database.js)
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3306
DATABASE_USERNAME=root
DATABASE_PASSWORD=root
DATABASE=libra
DB_SSL=false
DB_SSL_CA=

# JWT do admin (obrigatório)
TOKEN_SECRET_ADMIN=troque_este_segredo_admin_local_32chars
TOKEN_EXPIRATION=7d

# API
PORT=3018
URL=http://localhost:3018

# Front (Vite)
CORS_ORIGIN=http://localhost:5173

# Seed do primeiro admin (documentação — valores usados em src/database/seeds)
# email: admin@libracredito.com.br
# senha: LibraAdmin@123
